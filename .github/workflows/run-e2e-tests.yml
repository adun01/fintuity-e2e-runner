name: E2E Tests

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Test environment'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - staging
          - preprod
      spec:
        description: 'Test spec (optional, leave empty to run all tests)'
        required: false
        type: string

jobs:
  e2e-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Send Telegram notification - Tests started
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" \
            -d "text=🚀 <b>E2E тесты запущены</b>%0AОкружение: <code>${{ inputs.environment || 'dev' }}</code>%0AURL: <code>${{ secrets.DEV_URL }}</code>" \
            -d "parse_mode=HTML"

      - name: Clone repository from Bitbucket
        run: |
          git clone --depth 1 https://${{ secrets.BITBUCKET_USERNAME }}:${{ secrets.BITBUCKET_API_TOKEN }}@bitbucket.org/fintuity-team/e2e-tests.git project
          cd project
          git log -1 --oneline

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.18.1'
          cache: 'npm'
          cache-dependency-path: project/package-lock.json

      - name: Cache Cypress binary
        uses: actions/cache@v4
        with:
          path: ~/.cache/Cypress
          key: cypress-${{ runner.os }}-${{ hashFiles('project/package-lock.json') }}
          restore-keys: |
            cypress-${{ runner.os }}-

      - name: Install dependencies
        working-directory: project
        run: npm ci

      - name: Run E2E tests
        id: tests
        continue-on-error: true
        working-directory: project
        env:
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_PASSWORD: ${{ secrets.GMAIL_PASSWORD }}
          CYPRESS_BASE_URL: ${{ secrets.DEV_URL }}
        run: |
          if [ -n "${{ inputs.spec }}" ]; then
            npm run e2e:portal:${{ inputs.environment || 'dev' }} -- --headless --spec "${{ inputs.spec }}" 2>&1 | grep -v "CertVerifyProcBuiltin" || true
          else
            npm run e2e:portal:${{ inputs.environment || 'dev' }} -- --headless 2>&1 | grep -v "CertVerifyProcBuiltin" || true
          fi

      - name: Upload screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: screenshots-${{ inputs.environment }}-${{ github.run_number }}
          path: project/screenshots/
          if-no-files-found: ignore
          retention-days: 7

      - name: Upload videos
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: videos-${{ inputs.environment }}-${{ github.run_number }}
          path: project/videos/
          if-no-files-found: ignore
          retention-days: 7

      - name: Upload requests logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: requests-logs-${{ inputs.environment }}-${{ github.run_number }}
          path: project/requests-logs/
          if-no-files-found: ignore
          retention-days: 7

      - name: Upload memory logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: memory-logs-${{ inputs.environment }}-${{ github.run_number }}
          path: project/portal-e2e/cypress/results/
          if-no-files-found: ignore
          retention-days: 7

      - name: Send Telegram notification - Tests passed
        if: steps.tests.outcome == 'success'
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" \
            -d "text=✅ <b>Тесты прошли успешно!</b>%0AОкружение: <code>${{ inputs.environment || 'dev' }}</code>%0A%0A📊 Отправляем Cypress логи" \
            -d "parse_mode=HTML"

      - name: Send Telegram notification - Tests failed
        if: steps.tests.outcome == 'failure'
        run: |
          # Берём первый скриншот и извлекаем название теста
          FIRST_SCREENSHOT=$(find project/screenshots -type f -name "*failed*.png" 2>/dev/null | head -1)

          if [ -z "$FIRST_SCREENSHOT" ]; then
            TEST_NAME="Неизвестный тест"
          else
            TEST_NAME=$(basename "$FIRST_SCREENSHOT" | sed 's/ -- .*//' | sed 's/ (failed)\.png$//')
          fi

          MESSAGE="❌ <b>Тест упал!</b>%0AОкружение: <code>${{ inputs.environment || 'dev' }}</code>%0A%0A<b>Тест:</b> <code>$TEST_NAME</code>%0A%0A📸 Скриншоты%0A🎥 Видео%0A📋 Логи%0A📊 Cypress логи"

          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" \
            -d "text=$MESSAGE" \
            -d "parse_mode=HTML"

      - name: Send screenshots to Telegram
        if: steps.tests.outcome == 'failure'
        run: |
          find project/screenshots -type f -name "*failed*.png" 2>/dev/null | head -5 | while IFS= read -r screenshot; do
            TEST_NAME=$(basename "$screenshot" .png | sed 's/ -- / - /g' | sed 's/ (failed)//')

            echo "📸 Sending: $TEST_NAME"

            curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendPhoto" \
              -F "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" \
              -F "photo=@\"$screenshot\"" \
              -F "caption=📸 $TEST_NAME" || echo "⚠️ Failed to send"

            sleep 1
          done

      - name: Send videos to Telegram
        if: steps.tests.outcome == 'failure'
        run: |
          echo "🎥 Sending videos..."
          
          find project/videos -type f -name "*.mp4" 2>/dev/null | head -3 | while IFS= read -r video; do
            VIDEO_SIZE=$(stat -c%s "$video" 2>/dev/null || stat -f%z "$video" 2>/dev/null)
            FILENAME=$(basename "$video" .mp4)
          
            # Telegram лимит 50MB = 52428800 bytes
            if [ "$VIDEO_SIZE" -lt 52428800 ]; then
              echo "🎥 Sending video: $FILENAME ($(($VIDEO_SIZE / 1048576))MB)"
          
              curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendVideo" \
                -F "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" \
                -F "video=@\"$video\"" \
                -F "caption=🎥 $FILENAME" || echo "⚠️ Failed to send video"
            else
              echo "⚠️ Video too large: $FILENAME ($(($VIDEO_SIZE / 1048576))MB) - skipping"
            fi
          
            sleep 2
          done

      - name: Send request logs to Telegram
        if: steps.tests.outcome == 'failure'
        run: |
          echo "📋 Sending request logs..."

          find project/requests-logs -type f -name "*.json" 2>/dev/null | while IFS= read -r logfile; do
            FILENAME=$(basename "$logfile")

            echo "📄 Sending log: $FILENAME"

            curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendDocument" \
              -F "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" \
              -F "document=@\"$logfile\"" \
              -F "caption=📋 Request log: $FILENAME" || echo "⚠️ Failed to send log"

            sleep 1
          done

      - name: Send Cypress logs to Telegram
        if: always()
        run: |
          echo "📄 Sending Cypress logs..."

          if [ -d "project/portal-e2e/cypress/results" ]; then
            find project/portal-e2e/cypress/results -type f \( -name "*.json" -o -name "*.log" -o -name "*.txt" \) 2>/dev/null | while IFS= read -r logfile; do
              FILENAME=$(basename "$logfile")

              echo "📊 Sending: $FILENAME"

              curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendDocument" \
                -F "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" \
                -F "document=@\"$logfile\"" \
                -F "caption=📊 Cypress log: $FILENAME" || echo "⚠️ Failed to send"

              sleep 1
            done
          fi

          if [ -d "project/portal-e2e/cypress/logs" ]; then
            find project/portal-e2e/cypress/logs -type f 2>/dev/null | while IFS= read -r logfile; do
              FILENAME=$(basename "$logfile")

              echo "📝 Sending: $FILENAME"

              curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendDocument" \
                -F "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" \
                -F "document=@\"$logfile\"" \
                -F "caption=📝 Log: $FILENAME" || echo "⚠️ Failed to send"

              sleep 1
            done
          fi

      - name: Fail workflow if tests failed
        if: steps.tests.outcome == 'failure'
        run: exit 1